import { extendTheme } from "native-base";

export const moneyliaTheme = extendTheme({
  colors: {
    // Add new color
    purpleBlue: {
      1: "#7476ED",
      2: "#3A3B7B",
    },
    neutral: {
      1: "#6F6D7B",
    },
  },
});
